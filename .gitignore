# Node Modules
# This ignores all node_modules directories anywhere in the project
# including /frontend/node_modules and /backend/node_modules
node_modules/

# Environment Variables
# This ignores all .env files anywhere in the project
# This is CRUCIAL for keeping your secrets out of GitHub!

# You can also be more specific if needed, but a single .env entry is usually enough
# /frontend/.env
# /backend/.env

# Build Output Directories
# These are generated when you build your frontend/backend for production
/frontend/dist/
/backend/dist/ # If your backend has a build step

# Log files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE specific files (e.g., VS Code, IntelliJ, Sublime Text)
.vscode/
.idea/
.sublime-project
.sublime-workspace

# OS generated files
.DS_Store # macOS
Thumbs.db # Windows